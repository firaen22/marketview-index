# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# ALPHA_VANTAGE_API_KEY: Required for fetching market data.
# Obtain from: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY="your_alpha_vantage_api_key_here"

# Vercel KV / Redis Database Configuration
# Obtain these from your Vercel Project Settings > Storage > KV
KV_URL="redis://your_kv_url"
KV_REST_API_URL="https://your_kv_rest_url"
KV_REST_API_TOKEN="your_kv_rest_api_token"
KV_REST_API_READ_ONLY_TOKEN="your_kv_read_only_token"

# Shared secret for write access to /api/present-slide and /api/present-pdf.
# When set, the server requires `x-api-key` on all POST/DELETE requests.
# The same value must be exposed to the client as VITE_PRESENT_API_KEY at build time.
# Leave unset to allow unauthenticated writes (dev only).
PRESENT_API_KEY=""
VITE_PRESENT_API_KEY=""
